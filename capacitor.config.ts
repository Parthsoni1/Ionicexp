import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.expense.starter',
  appName: 'MyExpense',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
